task: 'retriever'

env:
  num_threads: 16
  seed: 42

dataset:
  name: 'cwq'
  text_encoder_name: 'gte-large-en-v1.5'

retriever:
  topic_pe: true
  DDE_kwargs:
    num_rounds: 2
    num_reverse_rounds: 2

optimizer:
  lr: 1e-3

eval:
  k_list: '5,10,30,50,100,200,400'

train:
  num_epochs: 10000
  patience: 10
  save_prefix: 'cwq'
